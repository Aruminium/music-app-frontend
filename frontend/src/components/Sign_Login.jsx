import React from "react";
import {View, Text} from "react-native";
import {Form, Item, Input, FormControl, Stack, Box} from "native-base";

const Sign_Login = () => {
  return (
		<View>
			<Text>aiueo</Text>
		</View>
	);
}

export default Sign_Login;